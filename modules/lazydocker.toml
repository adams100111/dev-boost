name     = "lazydocker"
category = "cli"
requires = []
verify   = "command -v lazydocker"

[install]
fedora = "sudo dnf install -y lazydocker"
debian = "sudo apt-get install -y lazydocker"
macos  = "brew install lazydocker"
