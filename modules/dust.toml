name     = "dust"
category = "cli"
requires = []
verify   = "command -v dust"

[install]
fedora = "sudo dnf install -y rust-dust"
debian = "sudo apt-get install -y dust"
macos  = "brew install dust"

[fallback]
mise = "aqua:bootandy/dust"
