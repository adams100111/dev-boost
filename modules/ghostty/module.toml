name        = "ghostty"
category    = "shell"
description = "Install Ghostty terminal (Fedora: scottames/ghostty COPR; Ubuntu: mkasberg/ghostty-ubuntu .deb); Ptyxis left as fallback"
requires    = []
profiles    = ["shell"]
gui         = true
verify      = "command -v ghostty"

[install]
fedora = "bash \"$DEVBOOST_ROOT/modules/ghostty/install.sh\""
debian = "bash \"$DEVBOOST_ROOT/modules/ghostty/install.sh\""
