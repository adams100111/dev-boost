name     = "lazygit"
category = "cli"
requires = []
verify   = "command -v lazygit"

[install]
fedora = "sudo dnf copr enable -y atim/lazygit && sudo dnf install -y lazygit"
debian = "sudo apt-get install -y lazygit"
macos  = "brew install lazygit"

[fallback]
mise = "aqua:jesseduffield/lazygit"
