name        = "starship"
category    = "shell"
description = "Install Starship cross-shell prompt binary (init line managed by dotfiles module)"
requires    = []
profiles    = ["shell"]
verify      = "command -v starship"

[install]
default = "bash \"$DEVBOOST_ROOT/modules/starship/install.sh\""

[fallback]
mise = "aqua:starship/starship"
