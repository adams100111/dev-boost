name     = "unzip"
category = "base"
requires = []
verify   = "command -v unzip"

[install]
fedora = "sudo dnf install -y unzip"
debian = "sudo apt-get install -y unzip"
macos  = "brew install unzip"
