name     = "sd"
category = "cli"
requires = []
verify   = "command -v sd"

[install]
fedora = "sudo dnf install -y sd"
debian = "sudo apt-get install -y sd"
macos  = "brew install sd"

[fallback]
mise = "aqua:chmln/sd"
