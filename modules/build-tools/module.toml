name     = "build-tools"
category = "base"
requires = []
verify   = "command -v gcc && command -v make && command -v cmake"

[install]
fedora = "sudo dnf install -y make automake gcc gcc-c++ kernel-devel cmake git wget perl vim nano unzip gnupg fastfetch unrar android-tools fuse-libs ripgrep"
