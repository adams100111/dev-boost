name     = "delta"
category = "cli"
requires = []
verify   = "command -v delta"

[install]
fedora = "sudo dnf install -y git-delta"
debian = "sudo apt-get install -y git-delta"
macos  = "brew install git-delta"
