# Supported-OS catalog — pinned ISO URLs + SHA256.
#
# This is the in-repo source of truth for what `devboost usb` can install (Principle III).
# Each sha256 MUST come from the distro's signed CHECKSUM — never invent one.
# Adding a distro or arch = one table below; it appears in the `devboost usb` wizard
# (friendly name) automatically, with zero code changes. The file is validated at load
# (structure + 64-hex sha256), so a malformed pin fails loudly.

[fedora-44]
name = "Fedora 44 — Workstation (Live)"
distro = "fedora"
version = "44"
edition = "Workstation-Live"

[fedora-44.isos.x86_64]
url = "https://download.fedoraproject.org/pub/fedora/linux/releases/44/Workstation/x86_64/iso/Fedora-Workstation-Live-44-1.7.x86_64.iso"
sha256 = "1620295f6a00c27c3208f0c00b8ece4eab1ec69b9002152d97488bf26a426ddf"

[fedora-44.isos.aarch64]
url = "https://download.fedoraproject.org/pub/fedora/linux/releases/44/Workstation/aarch64/iso/Fedora-Workstation-Live-44-1.7.aarch64.iso"
sha256 = "162ba3c552a2d241c7c63ec26777af0255ee1b5a135adc0be986ceed999933ef"
